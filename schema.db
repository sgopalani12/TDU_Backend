CREATE TABLE jeans (
    id SERIAL PRIMARY KEY,
    title VARCHAR(100),
    waist INTEGER,
    length INTEGER,
    color VARCHAR(50),
    price DECIMAL(6,2)
);

ALTER TABLE jeans ADD COLUMN image_link TEXT;
